<nav class="col-12 navbar navbar-expand-lg navbar-light bg-primary">
    <a class="navbar-brand text-secondary" href="{{ path('app_index') }}">Wild Series</a>
    <button class="navbar-toggler text-secondary" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link text-secondary" href="{{ path('app_index') }}">Accueil</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link text-secondary" href="{{ path('wild_index') }}">Séries</a>
            </li>
        </ul>
    </div>
</nav>